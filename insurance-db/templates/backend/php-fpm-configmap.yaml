apiVersion: v1
kind: ConfigMap
metadata:
  name: php-fpm-config
data:
  www.conf: |
    php_admin_value[error_log] = /dev/stderr
    php_admin_flag[log_errors] = on
    env[TZ] = $TZ
    user = www-data
    group = www-data
    pm = dynamic
    pm.max_children = 10
