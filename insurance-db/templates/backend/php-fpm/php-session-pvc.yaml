apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: insurance-db-php-session
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
  storageClassName: "nfs-ssd"
